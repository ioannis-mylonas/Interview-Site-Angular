import { ValidaIdadeDirective } from './valida-idade.directive';

describe('ValidaIdadeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaIdadeDirective();
    expect(directive).toBeTruthy();
  });
});
