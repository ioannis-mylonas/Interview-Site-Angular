import { ValidaFoneDirective } from './valida-fone.directive';

describe('ValidaFoneDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaFoneDirective();
    expect(directive).toBeTruthy();
  });
});
